'use client'

import styled from '@emotion/styled'

export const TipsControlPanel = styled.div({
  width: '100%',
  display: 'grid',
  gridTemplateColumns: 'repeat(4, 1fr)', // 4 equal columns per row
  gap: '20px',
})
