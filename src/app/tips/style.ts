'use client'

import styled from '@emotion/styled'

export const TipsControlPanel = styled.div(() => {
  return {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    flexWrap: 'wrap',
    height: '100%',
    gap: '20px',
    width: '100%',
  }
})
