import React from 'react'
import { Container } from './style'

export default function Panel({ proportion, minWidth }: any) {
  return <Container proportion={proportion} minWidth={minWidth}></Container>
}
